import React from "react";
import ReactDOM from "react-dom/client";
import PingtreeRoApp from "./PingtreeRoApp";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <PingtreeRoApp />
  </React.StrictMode>,
);
