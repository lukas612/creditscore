import React from "react";
import ReactDOM from "react-dom/client";
import MultipingRoApp from "./MultipingRoApp";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <MultipingRoApp />
  </React.StrictMode>,
);
