import React from "react";
import ReactDOM from "react-dom/client";
import PingtreeApp from "./PingtreeApp";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <PingtreeApp />
  </React.StrictMode>,
);
