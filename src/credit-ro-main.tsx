import React from "react";
import ReactDOM from "react-dom/client";
import CreditRoApp from "./CreditRoApp";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <CreditRoApp />
  </React.StrictMode>,
);
